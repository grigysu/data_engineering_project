# --- Ingestion ---
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
python-dotenv>=1.0

# --- Object store / lake I/O ---
boto3>=1.34
pyarrow>=15.0
pandas>=2.2

# --- Warehouse client ---
psycopg2-binary>=2.9
SQLAlchemy>=2.0

# --- Spark (local IDE typing + unit tests; real Spark runs in Docker) ---
pyspark==3.5.1

# --- ML ---
torch>=2.2
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8

# --- Dashboard (Phase 2c: Streamlit replaces FastAPI as the single UI) ---
streamlit>=1.32
plotly>=5.20

# --- Dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.3
