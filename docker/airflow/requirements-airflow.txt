# Runtime deps for Airflow tasks — Ingestion + ML only.
# Spark stays in its own container; Airflow calls `docker exec` for spark-submit.

# Ingestion
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
python-dotenv>=1.0
PyYAML>=6.0

# Object store + parquet
boto3>=1.34
pyarrow>=15.0
pandas>=2.2

# Warehouse (Phase 2b: actuals backfill + model registry from Airflow tasks)
psycopg2-binary>=2.9

# ML (CPU torch in Airflow — training in the DAG is a pipeline check;
# heavy training is run on the host where the GPU lives)
torch>=2.2
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
