# Streamlit dashboard runtime deps. CPU torch is installed separately in the
# Dockerfile so it doesn't pull the CUDA wheel.

# Ingestion (the dashboard imports ingestion.coverage)
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
python-dotenv>=1.0

# Object store + parquet (predictor + coverage scan)
boto3>=1.34
pyarrow>=15.0
pandas>=2.2

# Warehouse
psycopg2-binary>=2.9

# ML stack (predictor)
numpy>=1.26
matplotlib>=3.8

# UI
streamlit>=1.32
plotly>=5.20
